node_modules/
dist/
*.tsbuildinfo
.env

# Cursor — ignore local debug logs; commit .cursor/mcp.json so MCP config is not lost on clone
.cursor/
.claude/
# ColGREP MCP — built locally (`pnpm install` in tools/colgrep-mcp runs prepare → tsc)
tools/colgrep-mcp/

# Local agent instructions (contains internal design rationale not for public consumption)
AGENTS.md
